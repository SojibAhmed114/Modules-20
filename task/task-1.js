// Task-1
// Take four parameters. Multiply the four numbers and then return the result.

function Multi(num1, num2, num3, num4) {
    const numMulti = num1 * num2 * num3 * num4;
    return numMulti;
}

const multiData = Multi(24, 25, 45, 85);
console.log(multiData); 

// output: 2295000

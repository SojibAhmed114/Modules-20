// page number 2 ; parameter details and addition(+) 

//parameter: function তৈরি করার সময় () এর ভিতরে যা লিখা হয় তাই প্যারামিটার । par ameter শুধু একটা না প্রয়োজন অনুসারে অনেক নেয়া যায়।
// Addition (+) with many parameter.

function sum(num1, num2, num3) {
    const total = num1 + num2 + num3;
    console.log(total); 
}
sum(14, 21,25); //output:60



// try to debug (not for notes)

function debugs(vale1, vale2, vale3) {
    const totlaValues = vale1 + vale2 + vale3;
    console.log(totlaValues); 
}

console.log("debug system in function"); 
debugs(54, 54, 212);
debugs(54, 24, 512);
debugs(1114, 44, 582);
